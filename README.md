# THE PATH | iPhone Mobile Build

Open `index.html` in a browser. For a true Home Screen app experience, host this folder on an HTTPS static host and open it in Safari, then Share → Add to Home Screen.

This build is an HTML5 mobile playable vertical slice. The Unreal project remains the long-term 3D production branch.
